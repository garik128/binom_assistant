# 🚀 Запуск Binom Assistant

## Основной способ запуска (Production & Development)

### Веб-сервер (рекомендуется)

```bash
cd binom_assistant
python run_web.py
```

Или через main.py с дополнительными опциями:

```bash
cd binom_assistant
python main.py                    # Production режим
python main.py --dev              # Development с hot reload
python main.py --host 0.0.0.0     # Слушать все интерфейсы
python main.py --port 8080        # Изменить порт
```

После запуска:
- Веб-интерфейс: http://127.0.0.1:8000
- API документация: http://127.0.0.1:8000/docs
- ReDoc: http://127.0.0.1:8000/redoc

### Что происходит при запуске веб-сервера:

1. Запускается FastAPI приложение (uvicorn)
2. Автоматически инициализируется планировщик задач (TaskScheduler)
3. Планировщик по расписанию запускает:
   - Ежедневный сбор данных (настраивается в Settings)
   - Недельную агрегацию статистики
   - Очистку старых данных
4. Можно вручную запустить сбор через веб-интерфейс: Settings → "Обновить данные сейчас"

---

## Dev-инструменты (для разработки и отладки)

### 1. CLI сборщик данных (расширенные опции)

```bash
python dev_collector_cli.py                      # Обычный сбор (7 дней)
python dev_collector_cli.py --initial            # Первичный сбор (30 дней)
python dev_collector_cli.py --initial --fast     # Быстрый сбор БЕЗ ПАУЗ
python dev_collector_cli.py --days 14            # Сбор за 14 дней
python dev_collector_cli.py --initial --days 60 --fast  # 60 дней без пауз
```

**Когда использовать:**
- Первичная загрузка данных
- Восстановление данных за большой период
- Отладка сборщика
- Тестирование с разными параметрами

### 2. Standalone планировщик (отдельный процесс)

```bash
python dev_scheduler_standalone.py
```

**Когда использовать:**
- Отладка планировщика задач
- Тестирование cron-расписаний
- Запуск планировщика отдельно от веб-сервера (не рекомендуется для production)

**⚠️ Примечание:** В production планировщик запускается автоматически с веб-сервером, отдельный запуск не требуется!

---

## Архитектура запуска

```
┌─────────────────────────────────────────────────────────────┐
│                    PRODUCTION SETUP                          │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  cd binom_assistant && python run_web.py                     │
│         │                                                     │
│         ├──> FastAPI Web Server (uvicorn)                    │
│         │    └──> http://127.0.0.1:8000                      │
│         │                                                     │
│         └──> TaskScheduler (автоматически)                   │
│              ├──> Daily data collection (по расписанию)      │
│              ├──> Weekly aggregation (по расписанию)         │
│              └──> Cleanup old data (по расписанию)           │
│                                                               │
│  Manual trigger через UI: Settings → "Обновить данные"       │
│                                                               │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                   DEVELOPMENT TOOLS                          │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  python dev_collector_cli.py --initial --days 60 --fast      │
│  └──> Ручной запуск сборщика с расширенными опциями         │
│                                                               │
│  python dev_scheduler_standalone.py                          │
│  └──> Отдельный планировщик (для отладки)                   │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## Точки входа (Entry Points)

### Основные (для пользователей)

| Файл | Назначение | Команда |
|------|-----------|---------|
| `binom_assistant/run_web.py` | Запуск веб-сервера | `cd binom_assistant && python run_web.py` |
| `binom_assistant/main.py` | Запуск с опциями | `cd binom_assistant && python main.py --dev` |

### Dev-инструменты (для разработчиков)

| Файл | Назначение | Команда |
|------|-----------|---------|
| `dev_collector_cli.py` | CLI сборщик данных | `python dev_collector_cli.py --initial` |
| `dev_scheduler_standalone.py` | Standalone планировщик | `python dev_scheduler_standalone.py` |

---

## Production развёртывание

### Systemd service (рекомендуется)

Создайте файл `/etc/systemd/system/binom-assistant.service`:

```ini
[Unit]
Description=Binom Assistant Web Service
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/binom_assistant/binom_assistant
Environment="PATH=/opt/binom_assistant/venv/bin"
ExecStart=/opt/binom_assistant/venv/bin/python run_web.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запуск:
```bash
sudo systemctl daemon-reload
sudo systemctl enable binom-assistant
sudo systemctl start binom-assistant
sudo systemctl status binom-assistant
```

### Supervisor (альтернатива)

```ini
[program:binom-assistant]
command=/opt/binom_assistant/venv/bin/python run_web.py
directory=/opt/binom_assistant/binom_assistant
user=www-data
autostart=true
autorestart=true
redirect_stderr=true
stdout_logfile=/var/log/binom-assistant.log
```

---

## Проверка работоспособности

```bash
# Проверить что веб-сервер запущен
curl http://127.0.0.1:8000/health

# Посмотреть логи
tail -f binom_assistant/logs/app.log

# Проверить что планировщик работает
# (в логах должны быть записи о scheduled jobs)
grep "Scheduled jobs" binom_assistant/logs/app.log
```

---