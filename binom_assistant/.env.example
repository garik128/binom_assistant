# Binom API
# IMPORTANT: BINOM_URL must include your unique API file (e.g., /index.php)
# This file name is unique for each Binom installation and should be kept private
# Examples:
#   BINOM_URL=http://tracker.example.com/index.php
#   BINOM_URL=https://my-tracker.com/index.php
BINOM_URL=http://your-tracker-ip-or-domain/your_api_file.php
BINOM_API_KEY=your_binom_api_key_here

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
# Base URL for Telegram alert links
# IMPORTANT: Telegram blocks localhost links! For clickable links in production, use:
#   - External IP: http://YOUR_VPS_IP:8000
#   - Domain: https://your-domain.com
# For local development (links won't be clickable): http://localhost:8000
TELEGRAM_ALERT_BASE_URL=http://localhost:8000

# OpenRouter AI Configuration
OPENROUTER_API_KEY=sk-or-v1-..
OPENROUTER_MODEL=openai/gpt-4.1-mini
# Max tokens для ответа модели (GPT-4.1-mini макс: 32000)
# Рекомендуется: 16000 для работы с большими данными от DB tools
OPENROUTER_MAX_TOKENS=16000
OPENROUTER_CONTEXT_MESSAGES=10

# Database
DB_TYPE=sqlite
DB_PATH=data/binom_assistant.db

# Application
DEBUG=False
LOG_LEVEL=INFO
PORT=3040

# Data Collection Settings
COLLECTOR_ENABLED=true
COLLECTOR_INTERVAL_HOURS=24
COLLECTOR_UPDATE_DAYS=7

# Timezone Settings
TIMEZONE=Europe/Moscow

# Authorization Settings
# ВАЖНО: Измените логин и пароль перед продакшеном!
AUTH_USERNAME=admin
AUTH_PASSWORD=admin
# JWT Secret - ОБЯЗАТЕЛЬНО установите случайную строку в продакшене!
# Сгенерировать можно командой: python -c "import secrets; print(secrets.token_urlsafe(32))"
AUTH_JWT_SECRET=CHANGE_ME_IN_PRODUCTION
AUTH_JWT_ALGORITHM=HS256
AUTH_JWT_EXPIRATION_MINUTES=1440

# CORS Settings
# В продакшене укажите конкретные домены через запятую
# Например: CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
CORS_ORIGINS=*

# === Docker Settings ===
# Имя проекта для docker-compose
COMPOSE_PROJECT_NAME=binom-assistant
